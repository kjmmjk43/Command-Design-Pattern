public class Command {
    protected Document document;

    public Command(Document document) {
        this.document = document;
        //this.execute();
    }

    public String execute(){
        //if command equals view, views document

        //if commands equals append, appends the string at the bottom under the rest of the text aka in the file array list

        //appendCommand(document);
        //passes in the document

        //if command equals writeCommand

        //if command equals saveCommand

        return "";
    }
}
